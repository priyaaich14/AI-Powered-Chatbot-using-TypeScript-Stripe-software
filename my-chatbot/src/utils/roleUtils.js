// Utility function to check if the user is admin
export const isAdmin = (role) => role === 'admin';

// Utility function to check if the user is a technician
export const isTechnician = (role) => role === 'technician';

// Utility function to check if the user is a regular user
export const isUser = (role) => role === 'user';
